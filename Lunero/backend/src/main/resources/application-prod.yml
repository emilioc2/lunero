spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false

  flyway:
    out-of-order: false

logging:
  level:
    root: WARN
    com.lunero: INFO
    org.springframework.security: WARN
    org.flywaydb: INFO
