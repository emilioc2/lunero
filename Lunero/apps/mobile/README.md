# apps/mobile

> **Phase 2 — Not yet implemented.**

This directory will contain the React Native (Expo) mobile app for iOS and Android.
It will share `packages/ui`, `packages/core`, and `packages/api-client` with the web app.
